unit NFSToolbartypes;

interface

type
  TNFSToolbarItem = (tbBold, tbItalic, tbUnderline, tbLeft, tbCenter, tbRight, tbJustify,
                    tbFontcolor, tbBackgroundcolor, tbParagraphBackground,
                    tbBullets, tbNumber, tbSave, tbOpen, tbPicture,
                    tbLargeEditor, tbTextHoch, tbTextTief, tbStandardFont,
                    tbFontFavoriten, tbIndentInc, tbIndentDec, tbLineSpace,
                    tbPrint, tbClipboard, tbReverse, tbCut, tbPaste, tbSpellCheck, tbFontbox,
                    tbFontsize, tbBevel);

implementation

end.
